# ⚡ 5分钟快速上手指南

## 🎯 目标
5分钟内启动系统并创建第一个任务！

---

## 第1步：启动数据库（30秒）

```bash
# 确保MySQL正在运行
# Windows: 
net start MySQL80

# 创建数据库
mysql -u root -p
CREATE DATABASE demo;
exit;
```

---

## 第2步：启动后端（1分钟）

```bash
cd spring-boot-demo
mvn spring-boot:run
```

✅ 看到 "Started SpringBootDemoApplication" 表示成功

---

## 第3步：启动前端（1分钟）

```bash
cd frontend-react
npm install  # 首次运行需要
npm run dev
```

✅ 看到 "Local: http://localhost:5173" 表示成功

---

## 第4步：注册账号（30秒）

打开浏览器访问：http://localhost:5173

或使用API：
```bash
curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "email": "admin@example.com",
    "password": "admin123"
  }'
```

---

## 第5步：登录（30秒）

```bash
curl -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }'
```

复制返回的 `token`，后续请求需要用到。

---

## 第6步：创建工作空间（30秒）

```bash
curl -X POST http://localhost:8080/api/v1/workspaces \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "我的团队",
    "slug": "my-team"
  }'
```

记住返回的 `workspaceId`。

---

## 第7步：创建项目（30秒）

```bash
curl -X POST http://localhost:8080/api/v1/projects \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "我的第一个项目",
    "key": "PROJ1",
    "workspaceId": 1,
    "projectType": "SCRUM"
  }'
```

记住返回的 `projectId`。

---

## 第8步：创建任务（30秒）

```bash
curl -X POST http://localhost:8080/api/v1/tasks \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "我的第一个任务",
    "description": "测试任务创建",
    "projectId": 1,
    "taskType": "FEATURE",
    "priority": "HIGH"
  }'
```

---

## 🎉 完成！

现在你可以：

1. 访问 http://localhost:5173 查看前端界面
2. 访问 http://localhost:8080/swagger-ui.html 查看API文档
3. 在看板上拖拽任务
4. 添加评论和@提醒
5. 查看报表和仪表板

---

## 📚 下一步

想了解更多功能？查看：
- `USER_GUIDE.md` - 完整功能指南
- `QUICK_START_GUIDE.md` - 详细启动指南
- `PROJECT_COMPLETION_SUMMARY.md` - 项目功能总览

---

## 🆘 遇到问题？

### 后端启动失败
- 检查MySQL是否运行
- 检查 `application-dev.properties` 中的数据库密码

### 前端无法连接
- 确认后端已启动（访问 http://localhost:8080/actuator/health）
- 检查CORS配置

### 端口被占用
```bash
# Windows
netstat -ano | findstr :8080
taskkill /PID <进程ID> /F
```

---

## 🚀 快速命令备忘

```bash
# 启动后端
cd spring-boot-demo && mvn spring-boot:run

# 启动前端
cd frontend-react && npm run dev

# 查看API文档
open http://localhost:8080/swagger-ui.html

# 查看前端
open http://localhost:5173

# 健康检查
curl http://localhost:8080/actuator/health
```

---

**祝你使用愉快！** 🎊
