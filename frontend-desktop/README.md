# 用户管理系统 - 桌面版

基于 Tauri + React 开发的用户管理系统桌面应用。

## 功能特性

- 🖥️ **跨平台桌面应用** - 支持 Windows、macOS、Linux
- 🔐 **JWT 身份认证** - 安全的用户登录验证
- 👥 **用户管理** - 完整的用户 CRUD 操作
- 📱 **桌面通知** - 系统级通知提醒
- 💾 **离线缓存** - 网络断开时可查看缓存数据
- 🎨 **现代化 UI** - 基于 Ant Design 的美观界面
- ⚡ **高性能** - Rust 后端 + React 前端

## 技术栈

### 前端
- **React 19** - UI 框架
- **Ant Design 6** - UI 组件库
- **Vite 7** - 构建工具
- **Axios** - HTTP 客户端

### 后端
- **Tauri 1.6** - 桌面应用框架
- **Rust** - 系统级编程语言
- **Reqwest** - HTTP 客户端库

## 开发环境要求

### 必需软件
1. **Node.js** (>= 18.0.0)
2. **Rust** (>= 1.60.0)
3. **Tauri CLI**

### 安装步骤

1. **安装 Node.js**
   ```bash
   # 下载并安装 Node.js
   # https://nodejs.org/
   ```

2. **安装 Rust**
   ```bash
   # Windows
   # 下载并运行 rustup-init.exe
   # https://rustup.rs/
   
   # macOS/Linux
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
   ```

3. **安装 Tauri CLI**
   ```bash
   npm install -g @tauri-apps/cli
   ```

4. **克隆项目并安装依赖**
   ```bash
   cd frontend-desktop
   npm install
   ```

## 开发指南

### 启动开发服务器

1. **确保后端服务运行**
   ```bash
   # 在 spring-boot-demo 目录下
   mvn spring-boot:run
   ```

2. **启动桌面应用开发模式**
   ```bash
   npm run tauri:dev
   ```

### 构建生产版本

```bash
# 构建桌面应用
npm run tauri:build
```

构建完成后，可执行文件位于：
- Windows: `src-tauri/target/release/user-management-desktop.exe`
- macOS: `src-tauri/target/release/bundle/dmg/`
- Linux: `src-tauri/target/release/bundle/deb/`

## 项目结构

```
frontend-desktop/
├── src/                    # React 源码
│   ├── components/         # React 组件
│   │   ├── Dashboard.jsx   # 仪表板
│   │   ├── LoginForm.jsx   # 登录表单
│   │   ├── Settings.jsx    # 设置页面
│   │   └── UserList.jsx    # 用户列表
│   ├── services/           # 服务层
│   │   └── tauriAPI.js     # Tauri API 封装
│   ├── App.jsx             # 主应用组件
│   ├── App.css             # 样式文件
│   ├── main.jsx            # 应用入口
│   └── index.css           # 全局样式
├── src-tauri/              # Tauri 后端
│   ├── src/                # Rust 源码
│   │   ├── commands.rs     # Tauri 命令
│   │   └── main.rs         # 主程序
│   ├── Cargo.toml          # Rust 依赖配置
│   ├── tauri.conf.json     # Tauri 配置
│   └── build.rs            # 构建脚本
├── package.json            # Node.js 依赖
├── vite.config.js          # Vite 配置
└── README.md               # 项目说明
```

## 核心功能

### 1. 用户认证
- JWT Token 认证
- 自动登录状态保持
- 安全退出登录

### 2. 用户管理
- 查看用户列表
- 创建新用户
- 编辑用户信息
- 删除用户

### 3. 系统功能
- 实时服务器状态检测
- 网络连接状态监控
- 离线数据缓存
- 桌面通知提醒
- 应用设置管理

### 4. 离线支持
- 自动缓存用户数据
- 离线模式下可查看历史数据
- 网络恢复后自动同步

## API 接口

桌面应用通过以下 Tauri 命令与后端通信：

- `login(request)` - 用户登录
- `get_users(token)` - 获取用户列表
- `create_user(token, user)` - 创建用户
- `update_user(token, id, user)` - 更新用户
- `delete_user(token, id)` - 删除用户
- `show_notification(title, body)` - 显示通知
- `check_server_status()` - 检查服务器状态
- `save_to_local_storage(key, value)` - 保存本地数据
- `load_from_local_storage(key)` - 加载本地数据

## 配置说明

### Tauri 配置 (tauri.conf.json)
- 窗口大小：1200x800 (最小 800x600)
- 权限配置：文件系统、通知、网络访问
- 打包配置：图标、应用信息、目标平台

### Vite 配置 (vite.config.js)
- 开发服务器端口：1420
- 构建输出目录：dist
- React 插件配置

## 故障排除

### 常见问题

1. **Rust 编译错误**
   ```bash
   # 更新 Rust 工具链
   rustup update
   ```

2. **Tauri CLI 未找到**
   ```bash
   # 重新安装 Tauri CLI
   npm install -g @tauri-apps/cli@latest
   ```

3. **后端连接失败**
   - 确保 Spring Boot 服务在 localhost:8080 运行
   - 检查防火墙设置
   - 验证网络连接

4. **构建失败**
   ```bash
   # 清理缓存并重新构建
   npm run clean
   npm install
   npm run tauri:build
   ```

### 调试模式

开发模式下，可以通过以下方式调试：

1. **前端调试**：浏览器开发者工具
2. **Rust 调试**：控制台日志输出
3. **网络调试**：检查 API 请求响应

## 部署说明

### Windows 部署
1. 运行 `npm run tauri:build`
2. 生成的 `.exe` 文件可直接分发
3. 可选：创建安装程序

### macOS 部署
1. 需要 Apple 开发者证书进行代码签名
2. 生成 `.dmg` 安装包
3. 可上传到 Mac App Store

### Linux 部署
1. 生成 `.deb` 或 `.rpm` 包
2. 可通过包管理器安装
3. 支持 AppImage 格式

## 贡献指南

1. Fork 项目
2. 创建功能分支
3. 提交更改
4. 发起 Pull Request

## 许可证

MIT License

## 联系方式

- 项目地址：[GitHub Repository]
- 问题反馈：[Issues]
- 技术支持：[Email]