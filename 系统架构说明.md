# 🏗️ 系统架构说明

## 📁 项目结构

```
项目根目录/
├── spring-boot-demo/          # 后端（Spring Boot）
│   ├── src/main/java/org/example/
│   │   ├── SpringBootDemoApplication.java  # 启动类
│   │   ├── controller/        # REST API控制器
│   │   ├── service/           # 业务逻辑层
│   │   ├── entity/            # JPA实体（数据库表）
│   │   ├── repository/        # 数据访问层
│   │   ├── dto/               # 数据传输对象
│   │   ├── config/            # 配置类
│   │   └── security/          # 安全配置
│   ├── src/main/resources/
│   │   ├── application-dev.properties  # 开发环境配置
│   │   └── db/migration/      # Flyway数据库迁移脚本
│   └── pom.xml                # Maven依赖配置
│
├── frontend-react/            # 前端（React + Vite）
│   ├── src/
│   │   ├── components/        # React组件
│   │   ├── pages/             # 页面组件
│   │   ├── services/          # API服务
│   │   ├── store/             # 状态管理（Zustand）
│   │   ├── hooks/             # 自定义Hooks
│   │   └── utils/             # 工具函数
│   ├── package.json           # npm依赖
│   └── vite.config.ts         # Vite配置
│
└── 文档/
    ├── USER_GUIDE.md          # 用户使用指南
    ├── QUICK_START_GUIDE.md   # 快速启动指南
    ├── 5分钟快速上手.md        # 极简上手指南
    └── 系统架构说明.md         # 本文档
```

---

## 🎯 技术栈

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Java | 17 | 编程语言 |
| Spring Boot | 3.3.4 | 应用框架 |
| Spring Data JPA | 3.3.4 | ORM框架 |
| Spring Security | 3.3.4 | 安全认证 |
| MySQL | 8.0+ | 关系数据库 |
| Redis | 7.0+ | 缓存（可选） |
| JWT | 0.12.3 | Token认证 |
| Flyway | - | 数据库迁移 |
| SpringDoc | 2.3.0 | API文档（Swagger） |
| Maven | 3.6+ | 构建工具 |

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 19.2.0 | UI框架 |
| TypeScript | 5.9.3 | 类型系统 |
| Vite | 7.2.4 | 构建工具 |
| Ant Design | 6.1.1 | UI组件库 |
| React Router | 7.11.0 | 路由管理 |
| Zustand | 5.0.10 | 状态管理 |
| React Query | 5.90.16 | 数据获取 |
| Axios | 1.13.2 | HTTP客户端 |
| Socket.io | 4.8.3 | WebSocket |
| ECharts | - | 图表库 |
| DnD Kit | - | 拖拽功能 |

---

## 🏛️ 系统架构

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      用户浏览器                          │
│                   (http://localhost:5173)               │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP/WebSocket
                     ▼
┌─────────────────────────────────────────────────────────┐
│                   前端 (React + Vite)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ 组件层   │  │ 状态管理 │  │ API服务  │  │ WebSocket│ │
│  │Components│  │ Zustand  │  │ Axios    │  │Socket.io │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
└────────────────────┬────────────────────────────────────┘
                     │ REST API / WebSocket
                     ▼
┌─────────────────────────────────────────────────────────┐
│              后端 (Spring Boot)                          │
│                (http://localhost:8080)                   │
│  ┌──────────────────────────────────────────────────┐  │
│  │              Controller层 (REST API)              │  │
│  │  - AuthController    - ProjectController          │  │
│  │  - TaskController    - SprintController           │  │
│  │  - CommentController - NotificationController     │  │
│  └────────────────┬─────────────────────────────────┘  │
│                   │                                      │
│  ┌────────────────▼─────────────────────────────────┐  │
│  │              Service层 (业务逻辑)                 │  │
│  │  - UserService       - ProjectService             │  │
│  │  - TaskService       - SprintService              │  │
│  │  - CommentService    - NotificationService        │  │
│  └────────────────┬─────────────────────────────────┘  │
│                   │                                      │
│  ┌────────────────▼─────────────────────────────────┐  │
│  │            Repository层 (数据访问)                │  │
│  │  - UserRepository    - ProjectRepository          │  │
│  │  - TaskRepository    - SprintRepository           │  │
│  └────────────────┬─────────────────────────────────┘  │
│                   │                                      │
└───────────────────┼──────────────────────────────────────┘
                    │ JDBC
                    ▼
┌─────────────────────────────────────────────────────────┐
│                    MySQL 数据库                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ users    │  │ projects │  │ tasks    │  │ sprints │ │
│  │ workspace│  │ boards   │  │ comments │  │ ...     │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                  Redis 缓存 (可选)                       │
│  - Session缓存   - 通知缓存   - 查询缓存                │
└─────────────────────────────────────────────────────────┘
```

---

## 🔐 安全架构

### 认证流程

```
1. 用户登录
   ↓
2. 后端验证用户名密码
   ↓
3. 生成JWT Token (包含userId, username, roles)
   ↓
4. 返回Token给前端
   ↓
5. 前端保存Token (localStorage)
   ↓
6. 后续请求携带Token (Authorization: Bearer <token>)
   ↓
7. 后端验证Token
   ↓
8. 提取用户信息，执行业务逻辑
```

### JWT Token结构

```json
{
  "header": {
    "alg": "HS256",
    "typ": "JWT"
  },
  "payload": {
    "sub": "admin",
    "userId": 1,
    "roles": ["USER", "ADMIN"],
    "iat": 1705305600,
    "exp": 1705392000
  },
  "signature": "..."
}
```

### 权限控制

- **工作空间级别**：OWNER > ADMIN > MEMBER > GUEST
- **项目级别**：PROJECT_LEAD > DEVELOPER > VIEWER
- **方法级别**：使用 `@PreAuthorize` 注解控制

---

## 💾 数据库设计

### 核心表结构

#### 1. 用户表 (users)
```sql
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 2. 工作空间表 (workspaces)
```sql
CREATE TABLE workspaces (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    slug VARCHAR(50) UNIQUE NOT NULL,
    description TEXT,
    owner_id BIGINT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (owner_id) REFERENCES users(id)
);
```

#### 3. 项目表 (projects)
```sql
CREATE TABLE projects (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    key VARCHAR(10) UNIQUE NOT NULL,
    description TEXT,
    workspace_id BIGINT NOT NULL,
    project_type VARCHAR(20) NOT NULL,
    status VARCHAR(20) DEFAULT 'ACTIVE',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (workspace_id) REFERENCES workspaces(id)
);
```

#### 4. 任务表 (tasks)
```sql
CREATE TABLE tasks (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(200) NOT NULL,
    description TEXT,
    project_id BIGINT NOT NULL,
    task_type VARCHAR(20) NOT NULL,
    priority VARCHAR(20) NOT NULL,
    status VARCHAR(20) DEFAULT 'TODO',
    assignee_id BIGINT,
    reporter_id BIGINT NOT NULL,
    estimated_hours DECIMAL(10,2),
    sprint_id BIGINT,
    epic_id BIGINT,
    milestone_id BIGINT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id),
    FOREIGN KEY (assignee_id) REFERENCES users(id),
    FOREIGN KEY (reporter_id) REFERENCES users(id),
    FOREIGN KEY (sprint_id) REFERENCES sprints(id),
    FOREIGN KEY (epic_id) REFERENCES epics(id),
    FOREIGN KEY (milestone_id) REFERENCES milestones(id)
);
```

### 表关系图

```
users ──┬─── workspaces (owner)
        ├─── workspace_members
        ├─── project_members
        ├─── tasks (assignee/reporter)
        ├─── comments
        ├─── notifications
        └─── time_entries

workspaces ─── projects

projects ──┬─── tasks
           ├─── boards
           ├─── sprints
           ├─── epics
           ├─── milestones
           └─── webhooks

tasks ──┬─── comments
        ├─── attachments
        ├─── time_entries
        ├─── task_dependencies
        └─── board_columns (多对多)

sprints ─── tasks

epics ─── tasks

milestones ─── tasks
```

---

## 🔄 API设计

### RESTful API规范

#### 基础URL
```
http://localhost:8080/api/v1
```

#### 认证
```
Authorization: Bearer <JWT_TOKEN>
```

#### 响应格式
```json
{
  "success": true,
  "message": "操作成功",
  "data": { ... },
  "timestamp": "2026-01-15T10:30:00Z"
}
```

#### 错误响应
```json
{
  "success": false,
  "message": "错误信息",
  "error": "详细错误",
  "timestamp": "2026-01-15T10:30:00Z"
}
```

### API端点分组

#### 1. 认证 (Auth)
- `POST /auth/register` - 注册
- `POST /auth/login` - 登录
- `POST /auth/logout` - 登出
- `GET /auth/me` - 获取当前用户

#### 2. 工作空间 (Workspaces)
- `GET /workspaces` - 列表
- `POST /workspaces` - 创建
- `GET /workspaces/{id}` - 详情
- `PUT /workspaces/{id}` - 更新
- `DELETE /workspaces/{id}` - 删除
- `POST /workspaces/{id}/members` - 添加成员

#### 3. 项目 (Projects)
- `GET /projects` - 列表
- `POST /projects` - 创建
- `GET /projects/{id}` - 详情
- `PUT /projects/{id}` - 更新
- `DELETE /projects/{id}` - 删除

#### 4. 任务 (Tasks)
- `GET /tasks` - 列表
- `POST /tasks` - 创建
- `GET /tasks/{id}` - 详情
- `PUT /tasks/{id}` - 更新
- `DELETE /tasks/{id}` - 删除
- `GET /tasks/search` - 搜索

#### 5. Sprint
- `GET /sprints` - 列表
- `POST /sprints` - 创建
- `POST /sprints/{id}/start` - 启动
- `POST /sprints/{id}/complete` - 完成
- `POST /sprints/{id}/tasks/{taskId}` - 添加任务

#### 6. 看板 (Boards)
- `GET /boards/{id}` - 获取看板
- `POST /boards/{id}/columns` - 创建列
- `PUT /boards/{id}/columns/{columnId}` - 更新列

#### 7. 评论 (Comments)
- `GET /tasks/{taskId}/comments` - 获取评论
- `POST /comments` - 添加评论
- `PUT /comments/{id}` - 更新评论
- `DELETE /comments/{id}` - 删除评论

#### 8. 通知 (Notifications)
- `GET /notifications` - 列表
- `GET /notifications/unread` - 未读通知
- `PUT /notifications/{id}/read` - 标记已读
- `PUT /notifications/read-all` - 全部已读

#### 9. 报表 (Reports)
- `GET /reports/projects/{id}/dashboard` - 项目仪表板
- `GET /reports/sprints/{id}/burndown` - 燃尽图
- `GET /reports/projects/{id}/velocity` - 速度图
- `GET /reports/projects/{id}/task-distribution` - 任务分布

#### 10. Backlog
- `GET /projects/{id}/backlog` - 获取Backlog
- `POST /backlog/reorder` - 重新排序
- `POST /epics` - 创建Epic
- `POST /epics/{id}/tasks` - 添加任务到Epic

---

## 🔌 WebSocket事件

### 连接
```javascript
const socket = io('http://localhost:8080', {
  auth: { token: 'JWT_TOKEN' }
});
```

### 事件列表

| 事件名 | 方向 | 说明 |
|--------|------|------|
| `task:created` | 服务器→客户端 | 任务创建 |
| `task:updated` | 服务器→客户端 | 任务更新 |
| `task:deleted` | 服务器→客户端 | 任务删除 |
| `comment:added` | 服务器→客户端 | 新评论 |
| `notification:new` | 服务器→客户端 | 新通知 |
| `sprint:started` | 服务器→客户端 | Sprint开始 |
| `sprint:completed` | 服务器→客户端 | Sprint完成 |
| `board:updated` | 服务器→客户端 | 看板更新 |

---

## 📊 性能优化

### 后端优化

1. **数据库连接池**
   - HikariCP配置
   - 最小连接：5
   - 最大连接：20

2. **查询优化**
   - 使用索引
   - 避免N+1查询
   - 使用分页

3. **缓存策略**
   - Redis缓存热点数据
   - 用户Session缓存
   - 查询结果缓存

4. **异步处理**
   - 通知异步发送
   - Webhook异步调用
   - 邮件异步发送

### 前端优化

1. **代码分割**
   - 路由懒加载
   - 组件按需加载

2. **状态管理**
   - Zustand轻量级状态
   - React Query缓存

3. **渲染优化**
   - 虚拟列表
   - 防抖节流
   - Memo优化

---

## 🧪 测试策略

### 后端测试

1. **单元测试**
   - Service层测试
   - Repository层测试

2. **集成测试**
   - Controller层测试
   - API端到端测试

3. **性能测试**
   - JMeter压力测试
   - 数据库性能测试

### 前端测试

1. **组件测试**
   - React Testing Library

2. **E2E测试**
   - Cypress

---

## 🚀 部署架构

### 开发环境
```
本地开发
├── MySQL (localhost:3306)
├── Redis (localhost:6379)
├── 后端 (localhost:8080)
└── 前端 (localhost:5173)
```

### 生产环境（建议）
```
负载均衡器 (Nginx)
├── 前端静态资源 (CDN)
├── 后端集群 (多实例)
│   ├── Spring Boot App 1
│   ├── Spring Boot App 2
│   └── Spring Boot App 3
├── 数据库集群
│   ├── MySQL Master
│   └── MySQL Slave (读写分离)
├── Redis集群
└── 文件存储 (MinIO/OSS)
```

---

## 📈 监控和日志

### 监控指标

1. **应用监控**
   - Spring Boot Actuator
   - JVM内存、CPU
   - 请求响应时间

2. **数据库监控**
   - 连接池状态
   - 慢查询日志
   - 死锁检测

3. **业务监控**
   - 用户活跃度
   - API调用量
   - 错误率

### 日志管理

1. **日志级别**
   - ERROR：错误日志
   - WARN：警告日志
   - INFO：信息日志
   - DEBUG：调试日志

2. **日志存储**
   - 本地文件
   - ELK Stack（生产环境）

---

## 🔧 配置管理

### 环境配置

- `application-dev.properties` - 开发环境
- `application-test.properties` - 测试环境
- `application-prod.properties` - 生产环境

### 配置项说明

```properties
# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/demo
spring.datasource.username=root
spring.datasource.password=your_password

# Redis配置
spring.data.redis.host=localhost
spring.data.redis.port=6379

# JWT配置
jwt.secret=your-secret-key
jwt.expiration=86400000

# 文件上传
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
```

---

## 📚 扩展功能（未来）

1. **移动端支持**
   - React Native App
   - 响应式设计

2. **AI功能**
   - 智能任务分配
   - 工时预测
   - 风险预警

3. **高级报表**
   - 自定义报表
   - 数据导出
   - 报表订阅

4. **第三方集成**
   - GitHub集成
   - GitLab集成
   - Jira导入

---

## 🎓 学习资源

### 后端
- [Spring Boot官方文档](https://spring.io/projects/spring-boot)
- [Spring Security文档](https://spring.io/projects/spring-security)
- [JPA/Hibernate文档](https://hibernate.org/orm/documentation/)

### 前端
- [React官方文档](https://react.dev/)
- [Ant Design文档](https://ant.design/)
- [Zustand文档](https://zustand-demo.pmnd.rs/)

---

**系统架构持续演进中...** 🚀
